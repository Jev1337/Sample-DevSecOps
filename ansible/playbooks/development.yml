---
# Development mode playbook using Docker Compose
- name: DevSecOps Development Mode (Docker Compose)
  hosts: localhost
  vars_files:
    - ../vars/main.yml
  roles:
    - prerequisites
    - docker
    - development
